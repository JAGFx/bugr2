{% extends 'base.html.twig' %}

{% block body %}
    {% import 'shared/_currency.macro.html.twig' as currency %}

    <div class="d-flex justify-content-between align-items-center text-primary mt-4 mb-2 mx-3">
        <div class="d-flex justify-content-start align-items-center">
            <span>Entré</span>
        </div>
        <div class="me-3 text-end">Valeur</div>
    </div>

    <a class="btn btn-xs rounded-circle create-button d-flex justify-content-center align-items-center bg-gradient-accent text-white" href="{{ path('back_entry_create') }}">
        <i class="fa-solid fa-wand-magic-sparkles"></i>
    </a>

    <ul class="list-group rounded">
        {% for entry in pagination %}
            <turbo-frame id="entry_list_item_{{ entry.id }}">
                {% include 'domain/entry/_item.html.twig' %}
            </turbo-frame>
        {% endfor %}
    </ul>

    <div class="mt-4 d-flex justify-content-center">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}