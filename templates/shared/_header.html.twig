<header class="sticky-top">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center w-100">
                <div class="text-center w-100">
                    <a class="navbar-brand" href="#">Navbar</a>
                </div>
                <button class="btn fs-2 border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <i class="fa-solid fa-paw"></i>
                </button>
            </div>
            <div class="collapse navbar-collapse bg-acrylic rounded px-2 mb-2" id="navbarSupportedContent">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('back_account_account_list', {enabled: true}) }}">Comptes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('back_budget_budget_list', {enabled: true}) }}">Budgets</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('back_entries_list') }}">Entrés</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('back_periodicentry_list') }}">Entrées périodique</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>